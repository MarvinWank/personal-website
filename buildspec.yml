version: 0.1

phases:
  install:
    commands:
      - echo Installing dependencies
      - npm install
  build:
    commands:
      - echo Building the project...
      - npm run build

artifacts:
  files:
    - '_site/**/*'  # Include all files
  base-directory: dist
  discard-paths: yes